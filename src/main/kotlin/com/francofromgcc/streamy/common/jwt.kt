package com.francofromgcc.streamy.common

/**
 * Validate a JWT.
 * @param token The raw token as a string
 * @return True if the token is valid, false otherwise
 */
fun validateJwt(token: String): Boolean {
    return true
}
